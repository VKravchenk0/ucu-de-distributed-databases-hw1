spring:
  application:
    name: hw1
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres

  sql:
    init:
      mode: always
      platform: postgres

server:
  port: 8080